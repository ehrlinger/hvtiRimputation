% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputation-class.R
\name{summary.hvti_imputation}
\alias{summary.hvti_imputation}
\title{Summarise what was imputed}
\usage{
\method{summary}{hvti_imputation}(object, ...)
}
\arguments{
\item{object}{An object of class \link{hvti_imputation}.}

\item{...}{Ignored.}
}
\value{
A data frame with one row per imputed variable: the variable name,
the number of values filled, the proportion of rows that is, and the
value used to fill them.
}
\description{
Reduces the record to a per-variable table. It is a view of
\code{\link[=imputed_matrix]{imputed_matrix()}}, never a replacement for it: a count cannot say whether a
particular row was filled.
}
\examples{
dat <- data.frame(age = c(50, 60, NA, 70), bmi = c(NA, 25, 27, 29))
summary(impute_mean(dat, vars = c("age", "bmi")))
}
