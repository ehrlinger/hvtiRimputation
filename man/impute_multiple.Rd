% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute-multiple.R
\name{impute_multiple}
\alias{impute_multiple}
\title{Fill missing values with multiple imputation}
\usage{
impute_multiple(
  data,
  vars,
  m,
  cc_vars = names(data),
  method = NULL,
  maxit = 5L,
  seed = NA_integer_
)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Character vector of columns to impute. \strong{Required, with no
default} -- see Divergences.}

\item{m}{Number of completed datasets to draw. \strong{Required, with no
default} -- for the same reason \code{vars} has none in \code{\link[=impute_mean]{impute_mean()}}: the
SAS corpus's \code{NIMPUTE} defaults disagree across macro copies, so there
is no single "the default" to inherit.}

\item{cc_vars}{Character vector of columns over which \code{complete_case_pass}
is evaluated, defaulting to every column of \code{data}. This is the listwise
deletion the imputation is standing in for, so it is usually the whole
analysis frame rather than just \code{vars}.}

\item{method}{\code{NULL} (the default) uses \code{mice}'s own per-column method
selection for every \code{vars} column. A named character vector overrides
specific columns by name; every column not named keeps its default.
There is no single unnamed string form.}

\item{maxit}{Number of chained-equations iterations. Passed to
\code{mice::mice()}.}

\item{seed}{Passed to \code{mice::mice()}. \code{NA} (the default) means \code{mice}
does not set one.}
}
\value{
An object of class \code{hvti_imputation_multi}.
\code{\link[=imputed_data]{imputed_data()}} with a required \code{imputation} argument gives one
completed data frame or all \code{m} stacked long, \code{\link[=imputed_matrix]{imputed_matrix()}} the
row-by-variable record of what was filled (identical across every
draw), and \code{\link[=imputation_provenance]{imputation_provenance()}} how it was produced. See
\link{hvti_imputation_multi} for the full shape.
}
\description{
Draws \code{m} completed datasets by chained-equations multiple imputation
(\code{mice}), and returns them together with a record of exactly what was
filled. This is the R form of \verb{PROC MI}, which the \code{mult_imput} macro
wraps.
}
\details{
Unlike \code{\link[=impute_mean]{impute_mean()}}, a categorical variable is a normal imputation
target here: \code{method} defaults to \code{mice::make.method(data[vars])},
\code{mice}'s own per-column dispatch (\code{pmm} for numeric, \code{logreg} for a
2-level factor, \code{polyreg} for an unordered factor with more than two
levels, \code{polr} for an ordered factor). \strong{There is no single method string
applied to every column.} A caller overriding specific columns passes a
named character vector naming only those columns; every other column
keeps its own default. Forcing one method across a mix of column types is
a silent, package-wide default of exactly the kind this package's design
avoids for \code{vars} and \code{m}, and it is not what \code{impute_mean()}'s single
completed dataset needs multiplied by \code{m} -- it needs \code{mice} making its
own per-column decision, \code{m} times.

\code{impute_multiple()} trusts the class of each \code{vars} column as the sole
signal for what kind of variable it is, and does not re-derive that from
its distinct-value count. Deciding a column's type is
\code{hvtiRutilities::r_data_types()}'s job (or the caller's own
\code{factor()}/\code{as.logical()}), done once, before imputation -- not a
second, independent guess made here that could silently disagree with the
first one.
}
\section{Divergences from SAS}{


\strong{\code{vars} and \code{m} are both required.} \verb{PROC MI} with no \code{VAR} statement
processes every numeric variable, and its \code{NIMPUTE} default varies by
macro copy in the corpus this package ports. Neither default is
inherited here, for the reason recorded in \code{impute_mean()}'s own
Divergences section.

\strong{No pooling.} This function returns \code{m} valid completed datasets and
their record. Combining them into a single inferential answer -- fitting
a model to each draw and pooling by Rubin's rules -- is the caller's job.
Multiple imputation is only multiple imputation once the results are
pooled, and that is separate work with its own verification; see the
package README.
}

\examples{
set.seed(42)
n <- 30
dat <- data.frame(
  age  = round(rnorm(n, 60, 8)),
  grp  = factor(sample(c("a", "b", "c"), n, replace = TRUE)),
  flag = sample(c(TRUE, FALSE), n, replace = TRUE)
)
dat$age[sample(n, 4)]  <- NA
dat$grp[sample(n, 4)]  <- NA
dat$flag[sample(n, 4)] <- NA

out <- impute_multiple(dat, vars = c("age", "grp", "flag"), m = 2,
                       maxit = 2, seed = 1)
imputed_data(out, imputation = 1)
imputed_data(out, imputation = "long")
imputed_matrix(out)

}
\seealso{
\code{\link[=impute_mean]{impute_mean()}} for single imputation -- there is deliberately no
one function that picks between them. \code{\link[=imputed_any]{imputed_any()}} and
\code{\link[=complete_case_pass]{complete_case_pass()}} for the two row-level columns the attrition
record consumes.
}
