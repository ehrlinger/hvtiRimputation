% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputation-class.R, R/imputation-multi-class.R
\name{imputation-accessors}
\alias{imputation-accessors}
\alias{imputed_data}
\alias{imputed_matrix}
\alias{imputed_any}
\alias{complete_case_pass}
\alias{analysis_pass}
\alias{kept_by_imputation}
\alias{imputation_provenance}
\alias{imputed_data.hvti_imputation_multi}
\title{Read an imputation record}
\usage{
imputed_data(x, ...)

imputed_matrix(x)

imputed_any(x)

complete_case_pass(x)

analysis_pass(x)

kept_by_imputation(x)

imputation_provenance(x)

\method{imputed_data}{hvti_imputation_multi}(x, imputation, ...)
}
\arguments{
\item{x}{An object of class \link{hvti_imputation} or \code{hvti_imputation_multi}.}

\item{...}{Passed on to methods. \code{imputed_data()}'s
\code{hvti_imputation_multi} method takes a required \code{imputation} argument
this way; see \code{\link[=impute_multiple]{impute_multiple()}}.}

\item{imputation}{For the \code{hvti_imputation_multi} method: which completed
dataset to return. A single integer in \code{1:m}, or \code{"long"} for all \code{m}
stacked together. \strong{Required, with no default} -- there is no
zero-argument form that hands back something a caller could average
cell by cell across the \code{m} draws. See \code{dev/specs/ 2026-09-23-impute-multiple-design.md} for why that specific silent
default would reproduce the exact bug this design was written against.}
}
\value{
\code{imputed_data()} a data frame; \code{imputed_matrix()} a logical matrix;
\code{imputed_any()}, \code{complete_case_pass()}, \code{analysis_pass()} and
\code{kept_by_imputation()} logical vectors, one element per row;
\code{imputation_indicators()} a data frame of logical columns;
\code{imputation_provenance()} a list.
}
\description{
Accessors for the object \code{\link[=impute_mean]{impute_mean()}} returns, and (with one exception)
for the object \code{\link[=impute_multiple]{impute_multiple()}} returns. \code{imputed_data()} is the
exception: \code{\link[=impute_multiple]{impute_multiple()}}'s result holds \code{m} completed datasets, so
\code{imputed_data(x, imputation)} requires a second argument there, with no
default -- see \code{\link[=impute_multiple]{impute_multiple()}}.
}
\details{
\code{imputed_any()} and \code{complete_case_pass()} are the two row-level columns the
attrition record consumes. They enter a CONSORT tracker as an \strong{annotation}
-- a stage that records something about the rows without removing any -- and
not as an exclusion. Rows kept only because a covariate was filled in are
not excluded, and they are not fully observed either, and a record with no
vocabulary for that third state will misreport one or the other.

\strong{Use \code{\link[=kept_by_imputation]{kept_by_imputation()}} for the counterfactual, not
\code{imputed_any(x) & !complete_case_pass(x)}.} That expression overcounts. It
is \code{TRUE} for a row that had a value filled but is \emph{still} incomplete
because a column outside \code{vars} is missing -- a row that never enters the
analysis at all, and so was not kept by anything.

The two agree only when imputation completes every row, which is why the
error is easy to miss: in the study the design was written against, the
analysis set was every row, so post-imputation completeness was uniformly
\code{TRUE} and the wrong formula gave the right answer.
}
\examples{
dat <- data.frame(age = c(50, 60, NA, 70), bmi = c(NA, 25, 27, 29))
out <- impute_mean(dat, vars = c("age", "bmi"))

imputed_any(out)
complete_case_pass(out)
analysis_pass(out)

# Rows in the analysis only because a covariate was filled:
sum(kept_by_imputation(out))

}
