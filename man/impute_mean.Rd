% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute-mean.R
\name{impute_mean}
\alias{impute_mean}
\title{Fill missing values with the variable mean}
\usage{
impute_mean(data, vars, cc_vars = names(data))
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Character vector of columns to impute. \strong{Required, with no
default} -- see Divergences.}

\item{cc_vars}{Character vector of columns over which \code{complete_case_pass}
is evaluated, defaulting to every column of \code{data}. This is the listwise
deletion the imputation is standing in for, so it is usually the whole
analysis frame rather than just \code{vars}.}
}
\value{
An object of class \code{hvti_imputation}. \code{\link[=imputed_data]{imputed_data()}} gives the
completed data frame, \code{\link[=imputed_matrix]{imputed_matrix()}} the row-by-variable record of
what was filled, and \code{\link[=imputation_provenance]{imputation_provenance()}} how it was produced. See
\link{hvti_imputation} for the full shape.
}
\description{
Replaces every missing value in \code{vars} with that variable's mean over its
non-missing values, and returns the filled data together with a record of
exactly which values were changed.
}
\details{
This is the R form of \verb{PROC STANDARD ... REPLACE} with no \verb{MEAN=}/\verb{STD=},
which is what the \code{imputsub} macro wraps. It is \strong{single} imputation: one
completed dataset, and no between-imputation variance. If you want multiple
imputation, call \code{impute_multiple()} -- there is deliberately no one
function that picks between them, because a function that quietly does one
when the caller expected the other is a worse failure than no function at
all.
}
\section{Divergences from SAS}{


\strong{\code{vars} is required.} \verb{PROC STANDARD} with no \code{VAR} statement processes
every numeric variable in the dataset. That default is not inherited here,
for the same reason \code{impute_multiple()} inherits no \code{m}: an imputation that
silently chose its own variable list is the failure the record in this
package exists to catch. A port that mean-imputes the \emph{wrong} variable list
still reaches the right row count and still looks correct; it does not
reach the right \code{sum(imputed_any())}.

\strong{\verb{PROC STANDARD MEAN=0 STD=1 REPLACE} is not this function.} That form
standardises \emph{and} fills -- in standardised units the fill value 0 is the
mean, so it is imputation too, but it also rescales every value including
the observed ones. It is not implemented here. Do not reach for
\code{impute_mean()} to reproduce a job that used it.

\strong{\code{BY}-group means are not implemented.} How much of the corpus imputes
within \code{BY} groups has not been measured, so this is a gap rather than a
decision.
}

\examples{
dat <- data.frame(age = c(50, 60, NA, 70), bmi = c(NA, 25, 27, 29))
out <- impute_mean(dat, vars = c("age", "bmi"))
imputed_data(out)
imputed_matrix(out)

}
\seealso{
\code{\link[=imputed_any]{imputed_any()}} and \code{\link[=complete_case_pass]{complete_case_pass()}} for the two row-level
columns the attrition record consumes. Multiple imputation
(\code{impute_multiple()}) is designed but not yet implemented; see the README.
}
