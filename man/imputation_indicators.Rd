% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputation-class.R
\name{imputation_indicators}
\alias{imputation_indicators}
\title{Missingness indicators, generated from the record}
\usage{
imputation_indicators(x, prefix = "imputed_")
}
\arguments{
\item{x}{An object of class \link{hvti_imputation}.}

\item{prefix}{Prepended to each imputed variable's name. Defaults to
\code{"imputed_"}.}
}
\value{
A data frame of logical columns, one per imputed variable, with
\code{nrow()} matching the imputed data.
}
\description{
Returns one logical column per imputed variable, \code{TRUE} where that
variable's value was filled. A caller reproducing a SAS analysis that
carried \verb{ms_*} indicators as model terms renames in one step:
\code{imputation_indicators(x, prefix = "ms_")}.
}
\details{
The indicators are generated rather than stored, and the SAS \code{ms_} naming is
not the default. A convention adopted for parity becomes permanent -- it
would be ours to maintain forever the moment a study depended on it -- so
the record stays the single source of truth and the name stays the caller's
choice.

The principle underneath is not negotiable and predates the naming: \strong{a
value that means "observed" and a value that means "filled in" must not be
the same value.} The indicators are how that survives into a model.
}
\examples{
dat <- data.frame(age = c(50, 60, NA, 70), bmi = c(NA, 25, 27, 29))
out <- impute_mean(dat, vars = c("age", "bmi"))
imputation_indicators(out)
imputation_indicators(out, prefix = "ms_")
}
